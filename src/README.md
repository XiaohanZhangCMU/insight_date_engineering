Run program with

python pharmacy_counting.py inp_data out_data ncores

Note:

program will use up all available processors by default. This can be changed in the future to allow parallel within flexible number of cores. 

